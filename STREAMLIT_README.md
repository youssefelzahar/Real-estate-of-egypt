# 🏠 تطبيق Streamlit لتصنيف العقارات المصرية

## 📋 المتطلبات

تأكد من تثبيت جميع المكتبات المطلوبة:

```bash
pip install -r requierments.txt
```

أو قم بتثبيت Streamlit مباشرة:

```bash
pip install streamlit pandas numpy scikit-learn joblib
```

## 🚀 تشغيل التطبيق

قم بتشغيل التطبيق باستخدام الأمر التالي:

```bash
streamlit run app.py
```

سيتم فتح التطبيق في المتصفح تلقائياً على العنوان: `http://localhost:8501`

## 📥 الملفات المطلوبة

تأكد من وجود الملفات التالية في نفس المجلد:

- `app.py` - ملف التطبيق الرئيسي
- `segmentation_model.pkl` - نموذج التصنيف المدرب
- `scaler.pkl` - ملف التطبيع (scaling)
- `egypt_real_estate_listings.csv` - ملف البيانات (اختياري - للعقارات المشابهة)

## 🎯 كيفية الاستخدام

1. **إدخال بيانات العقار:**
   - العنوان (مثال: التجمع الخامس)
   - السعر بالجنيه المصري
   - المساحة بالمتر المربع
   - عدد غرف النوم
   - عدد الحمامات

2. **اضغط على زر "تصنيف العقار"**

3. **عرض النتائج:**
   - رقم الفئة (Segment Number: 0, 1, 2, أو 3)
   - وصف الفئة
   - السعر المتوقع
   - مقارنة السعر مع متوسط الفئة
   - العقارات المشابهة (إن وجدت)

## 📊 الفئات (Segments)

- **الفئة 0**: عقارات عائلية ميسورة التكلفة (Affordable Family Homes)
- **الفئة 1**: عقارات فاخرة جداً (Ultra-Luxury Estates)
- **الفئة 2**: قصور فاخرة متميزة (Premium Luxury Mansions)
- **الفئة 3**: عقارات مدمجة بميزانية محدودة (Compact Budget Properties)

## 🔧 استكشاف الأخطاء

إذا واجهت مشاكل:

1. **خطأ في تحميل النموذج:**
   - تأكد من وجود ملفات `.pkl` في نفس المجلد
   - تحقق من أن الملفات غير تالفة

2. **خطأ في تحميل البيانات:**
   - ملف CSV اختياري - التطبيق يعمل بدونه
   - بدون CSV لن يتم عرض العقارات المشابهة

3. **مشاكل في التثبيت:**
   - استخدم بيئة virtual environment
   - تأكد من استخدام Python 3.7 أو أحدث

## 📝 ملاحظات

- النموذج تم تدريبه على بيانات مجمعة على مستوى المجمعات (compounds)
- يمكن استخدامه للعقارات الفردية أيضاً
- النتائج تعتمد على الخصائص المدخلة (السعر، المساحة، الغرف، الحمامات)

